POSTGRES_USER=admin
POSTGRES_PASSWORD=secret
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=secret

# Сгенерировать: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-with-at-least-32-char-random-string

ADMIN_EMAIL=admin@reval.ru
ADMIN_PASSWORD=replace-with-strong-password

# SMTP — реальная почта для отправки писем верификации
# Gmail:   SMTP_HOST=smtp.gmail.com  SMTP_PORT=587
# Yandex:  SMTP_HOST=smtp.yandex.ru  SMTP_PORT=465
# Mail.ru: SMTP_HOST=smtp.mail.ru    SMTP_PORT=465
SMTP_HOST=imap.yandex.ru
SMTP_PORT=465
SMTP_USER=polonkoev.bers@yandex.ru
SMTP_PASSWORD=leceyimgnzldiavq
SMTP_FROM=polonkoev.bers@yandex.ru

# Базовый URL сервиса (используется в ссылках писем)
FRONTEND_URL=http://localhost:8000

# Опционально (есть умолчания для Docker)
# REDIS_URL=redis://redis:6379/0
# ML_SERVICE_URL=http://ml-service:8001
# CORS_ORIGINS=http://localhost:3000,http://localhost:8501
